# Directory Structure for Functions

All ISO-compliant functions are located in the Standard directory.
All functions specific to a database engine are located in the directory bearing its name.
When a function is non-ISO compliant but shares the same behavior and name across multiple servers, it is located in the Common directory.
